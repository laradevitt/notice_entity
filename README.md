# Notice entity

This module provides a 'notice' entity type that can be used to display time-sensitive messages.

Important
---------
* This module is *not* vetted by the community for production so use at your own risk.

Provides
--------
* Entity: Notice (notice)
* Block plugin: NoticeAlertBlock
* Controller: REST endpoint for accessing entity data via JavaScript
* Controller: Clone entity values into create form
